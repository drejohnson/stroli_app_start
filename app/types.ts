export interface Show {
  title: string;
  image: string;
  link: string;
}

export interface Step {
  title: string;
  description: string;
  image: string;
  alt: string;
  yTransform: [number, number];
}
